THEME_NAME=DeLorean
 
themedir = $(datadir)/themes/$(THEME_NAME)/gtk-2.0/Range

theme_DATA = \
	scale-slider.svg \
	scale-slider-hover.svg \
	scale-slider-insensitive.svg \
	trough-border.svg \
	trough-border-v.svg

EXTRA_DIST = $(theme_DATA)
