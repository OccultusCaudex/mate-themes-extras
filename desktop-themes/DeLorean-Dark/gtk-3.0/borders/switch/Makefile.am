THEME_NAME=DeLorean-Dark
 
themedir = $(datadir)/themes/$(THEME_NAME)/gtk-3.0/borders/switch

theme_DATA = \
	switch-slider-active.svg \
	switch-slider-backdrop.svg \
	switch-slider-insensitive.svg \
	switch-slider.svg

EXTRA_DIST = $(theme_DATA)
